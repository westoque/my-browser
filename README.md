# My Browser

A project that would autonomously build a browser from scratch using python and anthropic API.

## Requirements

- Python 3

## Getting Started

1. Install dependencies: `pip install -r requirements.txt`
2. Run orchestrator agent: `python run.py`
